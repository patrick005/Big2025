# Flask 웹 프레임워크
Flask==2.3.3

# SQLAlchemy - ORM
Flask-SQLAlchemy==3.1.1

# 요청 처리 (Kakao/Naver API 포함 시 사용)
requests==2.31.0

# 이미지 처리
Pillow==10.2.0

# OpenCV - 이미지 합성 및 pose 이미지 처리
opencv-python==4.9.0.80

# MediaPipe - pose landmark 추출
mediapipe==0.10.9

# Selenium - 네이버 쇼핑 이미지 크롤링
selenium==4.21.0

# 웹 드라이버 (크롬용) → 시스템 설치 권장, pip 경로 설치 가능 시:
# webdriver-manager==4.0.1  # optional

# 환경에 따라 추가 설치 필요할 수 있음:
# python-dotenv==1.0.1  # 환경변수 사용 시
