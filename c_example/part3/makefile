TARGET = swap selection bubble qsort pointerConcept endian pointer2

all : ${TARGET}

swap : $@
selection:$@
bubble : $@
qsort : $@
pointerConcept: $@
endian : $@
pointer2 : $@

clean : 
	rm ${TARGET}
